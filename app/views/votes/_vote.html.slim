- if votable.voted?(current_user) && votable.vote(current_user).status != status
  = render 'votes/form', votable: votable, status: status, disabled: true
- else
  = render 'votes/form', votable: votable, status: status
