<% if answer.errors.present? %>
  $('.answer[data-answer-id='+ <%= answer.id %> +'] .errors').html('<%= render 'shared/errors', resource: answer %>');
<% else %>
  $('.answer[data-answer-id='+ <%= answer.id %> +']').replaceWith('<%= j render answer %>');
<% end %>
